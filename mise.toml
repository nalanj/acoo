[tools]
go = "latest"

[tasks.build]
run = "go build -o ./acoo ./cmd/acoo/"
description = "Build acoo binary"

[tasks.install]
run = "go build -o ~/.local/bin/acoo ./cmd/acoo/"
description = "Build and install acoo to ~/.local/bin/"

[tasks.test]
run = "go test ./..."
description = "Run tests"
